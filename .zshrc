alias -g l="ls -l"
alias -g la="ls -a"
alias -g lla="ls -la"

alias -g gal="gcloud auth login"

alias -g k="kubectl"
alias -g kcx="kubectx"
alias -g kxc="kubectx -c"
alias -g kbs="kubens"
alias -g ksc="kubens -c"